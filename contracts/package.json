{
  "name": "bch-pay-contracts",
  "version": "1.0.0",
  "description": "CashScript smart contracts for BCH Pay - Bitcoin Cash payment platform",
  "type": "module",
  "scripts": {
    "compile": "tsx src/compile.ts",
    "deploy": "tsx src/deploy.ts",
    "deploy:dry": "tsx src/deploy.ts --dry-run",
    "interact": "tsx src/interact.ts",
    "build": "tsc --noEmit",
    "test": "tsx src/test-contracts.ts"
  },
  "dependencies": {
    "@cashscript/utils": "^0.10.0",
    "cashc": "^0.12.1",
    "cashscript": "^0.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
